[project]
name = "orchestrix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boxlite>=0.9.5",
    "langgraph>=1.2.1",
    "python-dotenv>=1.2.2",
    "rich>=14.0.0",
]
